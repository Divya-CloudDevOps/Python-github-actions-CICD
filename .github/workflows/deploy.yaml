name: Deploy Flask App

on:
  push:
    branches:
      - main  # Runs when you push to the main branch

jobs:
  deploy:
    runs-on: ubuntu-latest  # Creates the Ubuntu environment

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install local dependencies (for testing)
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          script: |
            # 1. Update system and install Python pip
            sudo apt update && sudo apt install python3-pip -y
            
            # 2. Go to app folder
            mkdir -p ~/flask_app && cd ~/flask_app
            
            # 3. Get latest code
            git pull origin main || git clone https://github.com/${{ github.repository }}.git .
            
            # 4. Install requirements with the force flag
            pip3 install -r requirements.txt --break-system-packages

           # 5. Stop old version and start new version properly
           pkill -f app.py || true
        
           # We use the full path to python3 to ensure it starts correctly
           nohup python3 app.py > log.txt 2>&1 &
        
           # Give the app 5 seconds to start so GitHub sees it is running
           sleep 5
           cat log.txt
